#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define vi vector<int>
#define umi unordered_map<int ,int>
const ll MOD = 1e9+7;

void solve(){
    int x,y,z;
    int matrix[x][y][y];
    for (int i = 0; i < x; i++)
    {
        for (int j = 0; j < y; j++)
        {
            for (int k = 0; k < z; k++)
            {
                cin >> matrix[i][j][k];
            }
            
        }
    }
    int current
    int answer[x][y][z];
    for (int i = 0; i < x; i++)
    {
        for (int i = 0; i < y; i++)
        {
            for (int k = 0; k < z; k++)
            {
                
            }
            
        }
        
    }
    

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int T;
    cin >> T;
    while(T--){
        solve();
    }
    return 0;
}