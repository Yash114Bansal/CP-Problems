#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int length, window;
    cin >> length >> window;
    int subarrays = length / window;
    if (subarrays * window < length)
        ++subarrays;
    int answer[subarrays][window];
    int start = 1;
    for (int i = 0; i < ; i++)
    {
        for (int i = 0; i < subarrays; i++)
        {
            answer[]
        }
        
    }
    

}

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;
}